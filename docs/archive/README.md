# 多页版归档

这里保留了 v1 的多页文档源（index/quickstart/syntax/components/examples），
仅作**历史参考**,不再对外发布。

- 当前唯一入口: `../index.html`(SPA shell, 单文件多路由)
- 修改文档请直接改 `../index.html`,不要改本目录下的文件
- assets(css/js/img)与主入口共享 `../assets/`
